import { useEffect } from "react";

export default function ToolBoxLanding(){
    useEffect(()=>{ },[])

    return (
        <div className="toolbox-page">
            <h1>Welcome to Tool Box Landing</h1>   
        </div>
    );
}